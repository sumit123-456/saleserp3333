spring.application.name=sales

# MySQL Configuration
spring.datasource.url=jdbc:mysql://crossover.proxy.rlwy.net:12360/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=btIuzZfUUpyMgCOuIAzhfPAvtsonoBhW
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server port (Railway dynamic port)
server.port=${PORT:9090}

# JWT Configuration
jwt.secret=mySuperSecretKeyForSalesERPApplication2024ThatIsAtLeast256BitsLong
jwt.expiration=86400000     # 24 hours in milliseconds
jwt.refresh.expiration=604800000   # 7 days in milliseconds

# Security
spring.security.oauth2.resourceserver.jwt.secret-key=${jwt.secret}

# Logging for debugging JWT
logging.level.com.sales.sales.security=DEBUG

